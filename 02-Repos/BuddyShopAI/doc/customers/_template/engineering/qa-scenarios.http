### ======================================================
### BuddyShopAI QA 情境測試（LINE Webhook）— 公版
###
### 使用方式：
###   1. 複製整個 customers/_template/ 到 customers/{tenant}/
###   2. 將下方 @baseUrl / @testUserId 改為該客戶的設定
###   3. 根據客戶驗收清單（qa.md）填入實際情境
###
### 驗收標準：customers/{tenant}/qa.md
### 一致性原則：新增情境時，三份文件同步更新：
###             1. customers/{tenant}/qa.md         （行為定義）
###             2. customers/{tenant}/engineering/qa-scenarios.http （手動觸發）
###             3. scripts/qa-smoke.sh               （CI 煙霧測試）
### ======================================================

@baseUrlProd    = https://{tenant}-func.azurewebsites.net/api
@baseUrlStaging = https://{tenant}-func-staging.azurewebsites.net/api
@baseUrl        = {{baseUrlStaging}}

@testUserId = Uxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
@signature  = REPLACE_WITH_VALID_SIGNATURE

# 簽章產生：
#   BODY='...'
#   SIG=$(echo -n "$BODY" | openssl dgst -sha256 -hmac "$LINE_CHANNEL_SECRET" -binary | base64)


# ==============================================================
# 01. {{分類名稱}}
# 驗收標準：customers/{tenant}/qa.md § 01
# ==============================================================

### [Q01] {{情境標題}}
# 預期：N 則訊息
#   必含：「{{關鍵詞}}」
#   不含：「{{禁止詞}}」
POST {{baseUrl}}/LineWebhook
Content-Type: application/json
X-Line-Signature: {{signature}}

{
  "destination": "Utest",
  "events": [
    {
      "type": "message",
      "message": { "type": "text", "id": "qa-Q01", "text": "{{客人問法}}" },
      "webhookEventId": "qa-Q01",
      "deliveryContext": { "isRedelivery": false },
      "timestamp": 1741305600000,
      "source": { "type": "user", "userId": "{{testUserId}}" },
      "replyToken": "qa-reply-Q01",
      "mode": "active"
    }
  ]
}

###
# 依此格式繼續新增情境（Q02, Q03, ...）
